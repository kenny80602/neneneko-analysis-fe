# 複製成 .env 後填入實際值：cp .env.example .env
# .env 已被 .gitignore 排除，切勿把真實值 commit 進版控。
#
# 註：正式環境不吃這支。部署時由容器啟動腳本依環境變數 API_BASE 產生 /config.js，
# 前端於執行期讀取（見 src/config.ts 與 public/config.js）。
# 本檔只影響「本機 npm start / npm run build」時 bundle 內的 fallback 值。

# 後端 API 位址。stock-market-analysis 的 web server 監聽 8081（見該專案 cmd/app.go）。
REACT_APP_API_BASE=http://localhost:8081
