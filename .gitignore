# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# editor
/.idea
*.iml

# code reviewer workflow artifacts
/.workflow

# 推內網 registry 的 build workflow。裡面有內網位址與 registry 帳號，
# 這個 repo 是公開的，進版控等於把內網拓樸送出去。
# 需要它的機器自己保留一份即可；改成部署 repo 之後再考慮收進來。
/.github/workflows/deploy.yml

# misc
.DS_Store
.env
!.env.example
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
